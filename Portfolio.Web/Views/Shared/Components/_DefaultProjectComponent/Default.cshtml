@model List<Category>
@*
   Default Project Component View
*@
<section id="portfolio" class="portfolio section">

    <!-- Section Title -->
    <div class="container section-title" data-aos="fade-up">
        <h2>Projelerim</h2>
        <p>Geliştirdiğim projelerime göz atabilirsiniz</p>
    </div><!-- End Section Title -->

    <div class="container">

        <div class="isotope-layout" data-default-filter="*" data-layout="masonry" data-sort="original-order">

            <!-- Kategoriler -->
            <ul class="portfolio-filters isotope-filters" data-aos="fade-up" data-aos-delay="100">
                <li data-filter="*" class="filter-active">Tümü</li>
                @foreach (var category in Model)
                {
                    <li data-filter=".filter-@category.CategoryId">@category.CategoryName</li>
                }
            </ul><!-- End Portfolio Filters -->
            <!-- Projeler -->
            <div class="row-gy-4 isotope-container" data-aos="fade-up" data-aos-delay="200">
                @foreach (var category in Model)
                {
                    @foreach (var project in category.Projects)
                    {
                        <div class="col-lg-4 col-md-6 portfolio-item isotope-item filter-@category.CategoryId">
                            <img src="@project.ImageUrl" class="img-fluid w-50" alt="">

                            <div class="portfolio-info">
                                <h4>@project.ProjectName</h4>
                                <p>@project.Description</p>

                                <!-- + ikonu ile açılan Lightbox -->
                                <a href="@project.ImageUrl"
                                   class="glightbox preview-link"
                                   data-title="@project.ProjectName"
                                   data-description="@project.Description <br><a href='@project.GithubUrl' target='_blank'><i class='bi bi-github'></i> GitHub Linki</a>">
                                    <i class="bi bi-zoom-in"></i>
                                </a>


                                <!-- Direkt GitHub linki (liste görünümünde de var) -->
                                <a href="@project.GithubUrl" title="Github Linki" class="details-link" target="_blank">
                                    <i class="bi bi-link-45deg"></i>
                                </a>
                            </div>
                        </div>
                    }
                }
            </div><!-- End Portfolio Container -->

        </div>

    </div>

</section>
